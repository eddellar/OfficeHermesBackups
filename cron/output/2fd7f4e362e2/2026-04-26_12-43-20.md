# Cron Job: Hermes配置备份

**Job ID:** 2fd7f4e362e2
**Run Time:** 2026-04-26 12:43:20
**Schedule:** 0 6 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

使用 git 增量备份 Hermes 用户配置到 GitHub。

【重要文件清单】（只追踪这些，存在才备份，不存在则跳过）：
- ~/.hermes/config.yaml
- ~/.hermes/.env
- ~/.hermes/skills/ （整个skills目录）
- ~/.hermes/cron/ （定时任务配置和执行记录）

【排除清单】（不追踪）：
- node_modules/
- __pycache__/
- *.pyc
- .git/
- venv/
- .cache/
- *.log
- *.tmp
- .DS_Store

【备份步骤】：

1. 【预检】创建临时目录 /tmp/hermes_git_backup
   - 如果创建失败，立即报告 "错误：无法创建临时目录 /tmp/hermes_git_backup" 并中止

2. 如果 /tmp/hermes_git_backup 不是 git 仓库（首次），则：
   - 初始化 git 仓库
   - 创建 .gitignore 排除上述清单
   - 添加 remote: https://eddellar:***@github.com/eddellar/OfficeHermesBackups.git
   - 首次需要 push --set-upstream origin main

3. 复制重要文件到 /tmp/hermes_git_backup（使用 rsync -av 排除无关文件）
   - 只复制存在的目录，不存在的目录跳过
   - 如果所有目录都不存在，报告错误并中止

4. 检查是否有变化（git status --porcelain）

5. 如果有变化：
   - git add -A
   - git commit -m "Backup YYYYMMDD"
   - git push
   - 如果 git push 失败（认证错误），立即报告 "错误：GitHub 认证失败，请检查 ~/.hermes/.env 中的 GITHUB_TOKEN 是否有效"
   - 报告：备份 X 个文件，变更内容摘要

6. 如果无变化：
   - 报告：无需备份，内容未变

7. 清理临时目录（即使前面失败也要清理）

8. 发送报告到 Feishu（无论是否有变化都要报告，除非预检失败则中止不发送）

注意：GITHUB_TOKEN 从 ~/.hermes/.env 读取

## Response

**Hermes 配置增量备份报告** ✅

📅 时间：2026-04-26 12:42

📦 备份结果：**成功**

**备份文件清单（561 个文件）：**
- `config.yaml` — 主配置文件
- `.env` — 环境变量（含 GITHUB_TOKEN）
- `cron/` — 定时任务配置及输出记录
- `skills/` — 完整 skills 目录（60+ skills）

**变更摘要：**
- 新增 561 个文件，190,102 行内容
- 首次完整备份，已推送至 `master` 分支
- GitHub 仓库：https://github.com/eddellar/OfficeHermesBackups
