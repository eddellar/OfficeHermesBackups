# Cron Job: Hermes垃圾清理

**Job ID:** 2fcbbe101235
**Run Time:** 2026-05-14 05:00:50
**Schedule:** 0 5 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

清理 Hermes 垃圾文件并报告结果。

执行以下清理操作：
1. 清理 ~/.hermes/tmp/ 下的临时文件（如果有）
2. 清理 ~/.hermes/logs/ 下超过7天的日志文件
3. 清理 ~/.hermes/cache/ 下超过7天的缓存文件
4. 清理 /tmp/hermes* 临时文件（WSL环境下）
5. 清理 ~/.cache/hermes/ 下的缓存（如果存在）
6. 清理 ~/.hermes/sessions/ 下超过90天的旧会话文件（*.json, *.jsonl），但保留 sessions.json

对于每个目录，统计清理前的文件数量和大小，然后报告：
- 清理了哪些目录
- 每个目录删除了多少文件、释放了多少空间
- 总共释放的空间

使用 du -sh 和 find 命令统计，使用 rm -rf 清理。
只删除明确是垃圾的文件，不要动配置文件和重要数据。

最后发送简短报告到 Feishu。

## Response

**Hermes 垃圾文件清理报告**

| 目录 | 操作 | 删除文件 | 释放空间 |
|------|------|----------|----------|
| `~/.hermes/tmp/` | 目录不存在 | - | - |
| `~/.hermes/logs/` | 删除超过7天的日志 | 2 个 | 19.9 KB |
| `~/.hermes/cache/` | 无过期缓存 | 0 个 | 0 B |
| `/tmp/hermes*` | 删除临时目录 | 2 个 | 11.4 KB |
| `~/.cache/hermes/` | 目录不存在 | - | - |
| `~/.hermes/sessions/` | 无超过90天的会话 | 0 个 | 0 B |

**清理结果汇总：**
- ✅ `~/.hermes/logs/`：删除 2 个旧日志文件，释放 19.9 KB
- ✅ `/tmp/hermes_sandbox_llq88ks3/`：删除临时目录，释放 11.4 KB
- ⏭️ `~/.hermes/sessions/`：无超过90天的会话文件（保留 sessions.json）

**总共释放空间：31.3 KB**
