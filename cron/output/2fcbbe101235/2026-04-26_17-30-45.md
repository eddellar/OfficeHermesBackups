# Cron Job: Hermes垃圾清理

**Job ID:** 2fcbbe101235
**Run Time:** 2026-04-26 17:30:45
**Schedule:** 0 5 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

清理 Hermes 垃圾文件并报告结果。

执行以下清理操作：
1. 清理 ~/.hermes/tmp/ 下的临时文件（如果有）
2. 清理 ~/.hermes/logs/ 下超过7天的日志文件
3. 清理 ~/.hermes/cache/ 下超过7天的缓存文件
4. 清理 /tmp/hermes* 临时文件（WSL环境下）
5. 清理 ~/.cache/hermes/ 下的缓存（如果存在）

对于每个目录，统计清理前的文件数量和大小，然后报告：
- 清理了哪些目录
- 每个目录删除了多少文件、释放了多少空间
- 总共释放的空间

使用 du -sh 和 find 命令统计，使用 rm -rf 清理。
只删除明确是垃圾的文件，不要动配置文件和重要数据。

最后发送简短报告到 Feishu。

## Response

🧹 **Hermes 垃圾清理报告** (2026-04-26)

---

## 📁 清理结果汇总

| 目录 | 状态 | 文件数 | 大小 |
|------|------|--------|------|
| `~/.hermes/tmp/` | 不存在 | - | - |
| `~/.hermes/logs/` | ✅ 无过期文件(全部<7天) | 4 | 412K |
| `~/.hermes/cache/` | ✅ 空目录(仅子目录结构) | 0 | 12K |
| `/tmp/hermes*` | ✅ 无(仅有运行中sock) | - | - |
| `~/.cache/hermes/` | 不存在 | - | - |

---

## 📋 详细说明

- **`~/.hermes/logs/`**：4个日志文件均在本周内（`agent.log`, `agent.log.bak`, `errors.log`, `update.log`），最旧的 `agent.log.bak` 修改于 Apr 21（距今5天），未超7天阈值，无需清理

- **`~/.hermes/cache/`**：包含 `documents/` 和 `screenshots/` 两个空子目录，无实际缓存文件

- **`/tmp/hermes*.sock`**：运行中的 RPC 套接字，为运行时依赖，不能删除

- **`/tmp/hermes_sandbox_*`**：运行时沙箱目录，已为空目录

- **`~/.cache/hermes/`**：目录不存在

---

## ✅ 结论

**无需清理** — 当前无过期垃圾文件

- 删除文件数：**0**
- 释放空间：**0 字节**

---

*Hermes 总目录大小：2.9G（主要为 hermes-agent 代码、venv 虚拟环境及 state.db）*
